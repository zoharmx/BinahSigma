# Binah-Σ Production Environment Variables
# Copy this file to .env.production and fill with real values

# CRITICAL: Generate a strong secret key for production
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=CHANGE_THIS_TO_STRONG_RANDOM_SECRET_IN_PRODUCTION

# LLM Provider API Keys
MISTRAL_API_KEY=your_mistral_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Environment
ENVIRONMENT=production

# Don't auto-generate demo keys in production
INIT_DEMO_KEYS=false

# Optional: Database connection (for future)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Optional: Redis connection (for rate limiting)
# REDIS_URL=redis://localhost:6379/0

# Optional: Sentry for error tracking
# SENTRY_DSN=your_sentry_dsn_here

# Optional: Application settings
# LOG_LEVEL=INFO
# WORKERS=2
# MAX_REQUESTS=1000
